# The directory which applications are bundled to
# e.g /my/bundle/directory
paths:
  bundle_directory: "/tmp"

# Email notification settings
# No smtp support for this version..
mail:
  host: your.server.ip
  from: example@example.com

# Deployment settings
deployment:
  OTA:
    url: http://url.com
